# Wifi_Passwords_Dumper
This script shows and saves all wifi networks and passwords a windows machine has ever connected to.

# How to run the script
1. Open PowerShell as root
2. Run this command:
   powershell -ExecutionPolicy Bypass -File ".\wifi_password_dumper.ps1"
